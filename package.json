{
  "name": "online-personal-profile",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix \"my Profile\" run dev",
    "build": "npm --prefix \"my Profile\" ci && npm --prefix \"my Profile\" run build",
    "preview": "npm --prefix \"my Profile\" run preview"
  }
}
